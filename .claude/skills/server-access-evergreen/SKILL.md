# Server Access

Use `/read-config-evergreen` to get the SSH aliases for staging and production.

Read `~/.ssh/config` for hostnames and connection details. Discover the process manager, deploy procedures, and database access patterns from the project repo.
