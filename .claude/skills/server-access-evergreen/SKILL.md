# TACOS Server Access

SSH aliases `tacos-staging` and `tacos-prod` are configured in `~/.ssh/config`. Both run the app via PM2. Read the SSH config for hostnames and keys. Read `/home/ben/Projects/TACOS/.claude/commands/tacosdb.md` for database access patterns. Check `scripts/deploy-staging.sh` in TACOS for deploy procedures.
